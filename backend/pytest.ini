[pytest]
addopts = -q --maxfail=1 --disable-warnings --strict-markers --cov=app --cov-report=term-missing
testpaths = tests
markers =
    perf: performance/synthetic tests (skipped in CI unless -m perf)
